<UserControl x:Class="BoardApp.Views.SignUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:BoardApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="450"
             TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Secondary.Dark}"
             Background="{x:Null}">

    <!--<md:Card Background="Transparent">

        <StackPanel>
            <Button Name="Close"
                    Command="{Binding CloseAppCommand}"
                    HorizontalAlignment="Right"
                    Margin="0 20 20 0"
                    Style="{DynamicResource MaterialDesignFlatButton}"       
                    Height="40">

                <md:PackIcon Kind="Close"/>
            </Button>
        </StackPanel>

    </md:Card>-->
    
</UserControl>
